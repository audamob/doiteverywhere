<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.audamob.doit"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />
    <!-- So that we can get the account name -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!-- Used to request data from the Google+ APIs in the SignInActivity -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required if we need to invalidate access tokens -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" 
        android:screenOrientation="portrait">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity       
            android:name="com.audamob.doit.activity.AuthenticationActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.audamob.doit.activity.MainContainerActivity"
            android:label="@string/second_title"
            android:logo="@drawable/ic_action_sliding_menu"
            android:theme="@style/CustomUpIndicatorTheme" />
        <activity
            android:name="com.audamob.doit.activity.ProfileActivity"
            android:label="@string/Profile_title"
            android:logo="@drawable/ic_action_back"
            android:theme="@style/CustomUpIndicatorTheme" />
          <activity
            android:name="com.audamob.doit.activity.DoItActivityDetails"
            android:label="@string/Details_Text"
            android:logo="@drawable/ic_action_back"
            android:theme="@style/CustomUpIndicatorTheme" />
    </application>

</manifest>